import { experiences } from '../data/portfolio'
export function Experience() { return <section id="experience" className="section" aria-labelledby="experience-title"><div className="section-heading"><span>EXPERIENCE & EDUCATION</span><h2 id="experience-title">경험과 교육 이력</h2></div><div className="timeline">{experiences.map((experience) => <article className="timeline-item" key={`${experience.period}-${experience.title}`}><time>{experience.period}</time><div><h3>{experience.title}</h3><strong>{experience.label}</strong>{experience.details.length > 0 && <ul>{experience.details.map((detail) => <li key={detail}>{detail}</li>)}</ul>}</div></article>)}</div></section> }
