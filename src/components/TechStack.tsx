import { useState } from 'react'
import { techCategories, technologies, type TechCategory } from '../data/portfolio'
export function TechStack() { const [selected, setSelected] = useState<TechCategory>('전체'); return <section id="tech" className="tech" aria-labelledby="tech-title"><div className="tech-heading"><span>기술 스택 및 도구</span><h2 id="tech-title">프로젝트에 사용한 기술입니다.</h2></div><div className="tabs" role="tablist" aria-label="기술 분류">{techCategories.map((category) => <button key={category} role="tab" aria-selected={selected === category} onClick={() => setSelected(category)}>{category}</button>)}</div><div className="tech-grid">{technologies.map((technology) => { const active = selected === '전체' || selected === technology.category; const stateStyle = { opacity: active ? 1 : 0.15, filter: active ? 'blur(0px)' : 'blur(6px)', transform: `scale(${active ? 1 : 0.95})` }; return <div key={technology.name} className="tech-item" title={technology.name} style={stateStyle}><img src={technology.icon} alt={technology.name} style={{ opacity: stateStyle.opacity }} /></div> })}</div></section> }
