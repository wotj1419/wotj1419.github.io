import { projects } from '../data/portfolio'

const projectTechIcons: Record<string, string> = { ReactJS: 'react/react-original.svg', VueJS: 'vuejs/vuejs-original.svg', TypeScript: 'typescript/typescript-original.svg', JavaScript: 'javascript/javascript-original.svg', Android: 'android/android-original.svg', Kotlin: 'kotlin/kotlin-original.svg', Vite: 'vitejs/vitejs-original.svg', TailwindCSS: 'tailwindcss/tailwindcss-original.svg', Zustand: 'https://raw.githubusercontent.com/pmndrs/zustand/main/docs/favicon.ico', Pinia: 'https://pinia.vuejs.org/logo.svg' }
const techIcon = (technology: string) => { const icon = projectTechIcons[technology]; return icon?.startsWith('http') ? icon : `https://cdn.jsdelivr.net/gh/devicons/devicon@latest/icons/${icon}` }

type ProjectsProps = { onSelect: (projectId: string) => void }
export function Projects({ onSelect }: ProjectsProps) { return <section id="projects" className="projects" aria-labelledby="projects-title"><div className="projects-heading"><span>FEATURED PROJECTS</span><h2 id="projects-title">주요 프로젝트</h2><p>각 프로젝트에서 마주한 문제, 해결 방식, 담당 역할을 중심으로 정리했습니다.</p></div><div className="project-list">{projects.map((project) => <article className="project-card" key={project.id}><button className="project-image-button" onClick={() => onSelect(project.id)} aria-label={`${project.title} 프로젝트 미리보기 열기`}><img src={project.heroImage} alt={`${project.title} 프로젝트 미리보기`} /></button><div className="project-info"><span className="tag">{project.id === 'itda' ? ' ' : ''}{project.badge}</span><h3>{project.title}</h3><p className={project.award ? 'project-subtitle has-award' : 'project-subtitle'}>{project.subtitle}</p>{project.award && <p className="project-award">수상 내역 · {project.award}</p>}<p className="project-description">{project.description}</p><div className="project-tech-icons">{project.tech.map((technology) => <img key={technology} src={techIcon(technology)} alt={technology} title={technology} />)}</div><button className="detail-button" onClick={() => onSelect(project.id)}>프로젝트 자세히 보기 →</button></div></article>)}</div></section> }
